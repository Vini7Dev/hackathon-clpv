import React from 'react';
import {
  Container,
  ButtonText,
} from './styles';

const FooterButton: React.FC = () => {
  return (
    <Container>
      <ButtonText>
        Entrar
      </ButtonText>
    </Container>
  );
};

export default FooterButton;
